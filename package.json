{
  "name": "registro-motos-full",
  "version": "1.0.0",
  "description": "Sistema de registro de motos robadas",
  "scripts": {
    "dev": "concurrently \"cd backend && node app.js\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node app.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}