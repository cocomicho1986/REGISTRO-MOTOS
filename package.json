{
  "name": "registro-motos-full",
  "scripts": {
    "dev": "concurrently \"cd backend && npm install && node app.js\" \"cd frontend && npm install && npm run dev\"",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node app.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
